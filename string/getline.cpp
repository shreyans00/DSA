#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    string s;
    getline(cin, s);
    // first getline store the /n that is stored after 4
    getline(cin, s);
    cout << s;
}